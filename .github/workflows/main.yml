name: Deploy

on:
  push:
    branches:
      - master
  pull_request:
    branches: [master]
    types: [opened, synchronize]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: running akhileshns/heroku-deploy@v3.12.12
        if: ${{ github.event_name == 'push' }}
        uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: 'protected-oasis-46554'
          heroku_email: 'ngockimtranle@gmail.com'
          healthcheck: 'https://protected-oasis-46554.herokuapp.com/health'
          checkstring: 'ok'
          rollbackonhealthcheckfailed: true
